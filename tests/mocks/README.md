# Mock класе

Дат је примјер класе `MockInventoryService`.

Студент треба да дода:

- `MockPaymentService.h`;
- `MockNotificationService.h`;
- `MockLogger.h`.

Mock класе треба користити за изоловано тестирање
класе `OrderService`.
